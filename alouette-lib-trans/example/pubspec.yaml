name: alouette_lib_trans_example
description: Example application demonstrating the alouette_lib_trans library.
version: 1.0.0+1

environment:
  sdk: ^3.8.1
  flutter: ">=3.8.1"

dependencies:
  flutter:
    sdk: flutter
  alouette_lib_trans:
    path: ../

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true